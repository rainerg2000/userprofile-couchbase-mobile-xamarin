<?xml version="1.0" encoding="UTF-8"?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:converters="clr-namespace:UserProfileDemo.Converters"
    x:Class="UserProfileDemo.Pages.UserProfilePage"
    Title="Your Profile"
    BackgroundColor="White">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Save" Command="{Binding SaveCommand}" />
    </ContentPage.ToolbarItems>
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:ByteToImageFieldConverter x:Key="byteToImageSource" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <Grid Margin="15,25,15,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width=".25*" />
                <ColumnDefinition Width=".25*" />
                <ColumnDefinition Width=".75*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackLayout Grid.Row="0" Grid.ColumnSpan="2" Spacing="5" Margin="0,0,0,20">
                 <Image Source="{Binding ImageData, Converter={StaticResource byteToImageSource}}"
                                 Aspect="AspectFit" WidthRequest="300" HeightRequest="200" HorizontalOptions="Center" />
                 <Button Text="Upload Image" Command="{Binding SelectImageCommand}" HorizontalOptions="Center" />
            </StackLayout>
            <Label Grid.Row="1" Grid.Column="0" Text="Name:" HorizontalOptions="End" VerticalOptions="Center" />
            <Entry Grid.Row="1" Grid.Column="1" Text="{Binding Name}" VerticalOptions="Center"  />
            <Label Grid.Row="2" Grid.Column="0" Text="Email:" HorizontalOptions="End" VerticalOptions="Center" />
            <Entry Grid.Row="2" Grid.Column="1" Text="{Binding Email}" VerticalOptions="Center" IsEnabled="false" />
            <Label Grid.Row="3" Grid.Column="0" Text="Address:" HorizontalOptions="End" VerticalOptions="Center" />
            <Entry Grid.Row="3" Grid.Column="1" Text="{Binding Address}" VerticalOptions="Center" />
            <Entry Grid.Row="4" Grid.Column="2" Text="{Binding SyncStatus}" VerticalOptions="Center" />
            <Label Grid.Row="4" Grid.Column="0" Text="Oneshot:" HorizontalOptions="End" VerticalOptions="Center" />
            <Button Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="1" Text="Sync" Command="{Binding SyncCommand}" VerticalOptions="End" />
            <Label Grid.Row="5" Grid.Column="0" Text="Periodic:" HorizontalOptions="End" VerticalOptions="Center" />
            <Switch Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="1" IsToggled="{Binding IsPeriodicallyReplicating}" />
            <Label Grid.Row="6" Grid.Column="0" Text="Continuous:" HorizontalOptions="End" VerticalOptions="Center" />
            <Switch Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="1" IsToggled="{Binding IsContinuouslyReplicating}" />
            <Entry Grid.Row="6" Grid.Column="2" Text="{Binding SyncStatusContinuous}" VerticalOptions="Center" />
            <Button Grid.Row="8" Grid.Column="1" Grid.ColumnSpan="1" Text="Logout" Command="{Binding LogoutCommand}" 
                    TextColor="Red" VerticalOptions="End" HorizontalOptions="Center" />
        </Grid>
    </ContentPage.Content>
</ContentPage>
